# balance-app 画面・API 一覧(自動生成）

> 再生成: `node tools/gen-app-map.mjs balance-app`。画面 1 / API 3。手で編集しない。

## 画面(1)

| パス | タイトル |
|---|---|
| `/` | — |

## API(3)

| エンドポイント | メソッド |
|---|---|
| `/api/collect` | POST |
| `/api/health` | GET |
| `/api/ready` | GET |
