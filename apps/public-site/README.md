# public-site

公開サイト基盤アプリ。`@platform/site`（ページ・メニュー・お知らせ・ブロック）、`@platform/seo`（メタ・OGP・JSON-LD・robots）、`@platform/ui` の `Markdown`(本文の描画)、`@platform/analytics`（計測ビーコン）を組み合わせる。

- コンテンツは `src/server/site-content.ts`（既定インメモリ。CMS/DB へ差し替え可能）。
- ページは `src/app/[slug]/page.tsx` がブロックから描画し、SEO メタ・パンくず・お知らせ・計測ビーコンを付与。
- `GET /robots.txt` は公開/非公開の可視性に応じて生成。

## 決めていること

- **本文を HTML として流し込まない。** 以前は `nl2br(linkify(...))` を
  `dangerouslySetInnerHTML` に渡していたが、CMS の編集者が書いた本文を
  サニタイズせずに描いていた。`Markdown` は React 要素に組み立てるので、
  文字列が HTML として解釈される経路が無い(2026-08 に変更)。
- **プレビューは公開中フィルタを通さない。** 下書き・予約公開を
  共有トークン付きで確認するため。トークンの照合は**定数時間**で行う
  (`===` だと一致した文字数で時間が変わり、1 文字ずつ絞り込める)。

## このアプリの運用

### 出し先

**まだ決まっていません。**

**公開サイトなので、社内アプリとは分けて**ください——
**同じサーバに置くと、外からの攻撃が社内アプリにも届きます**。

### 試験の状況

| 種類 | 状況 |
|---|---|
| smoke | 基盤側で全体を確認しています |
| E2E | **未整備** |

### 出す前に確認すること

- **誤字**（直せます）と**取引先の名前**（**間違えると取り返しがつきません**）
- `robots.txt` と `noindex` の設定——**社内向けの下書きが載らないか**
