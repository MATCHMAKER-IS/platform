# equipment-app の環境変数。既定はインメモリ。初期ログイン: admin@example.com / ADMIN_PASSWORD
SESSION_SECRET=dev-secret-equipment-change-me
ADMIN_PASSWORD=admin1234
# PostgreSQL を使う場合(備品・貸出履歴が永続化。DB "app_equipment" は setup.sh が作成済み)
# PERSISTENCE=prisma
# DATABASE_URL=postgresql://app:app@localhost:5432/app_equipment
