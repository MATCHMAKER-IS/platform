# crud-template の環境変数。

# --- 必須 ---
# このアプリは **PostgreSQL を使う前提**です。
# DB は setup（scripts/setup.ps1 / setup.sh）が `app_crud` を作成します。
DATABASE_URL=postgresql://app:app@localhost:5432/app_crud

# --- 任意 ---
# 実験用にメモリで動かしたいときだけ指定します。
# **再起動で消える**ので、開発の使い捨て以外には向きません。
# PERSISTENCE=memory
