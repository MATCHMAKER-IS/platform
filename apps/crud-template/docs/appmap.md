# crud-template 画面・API 一覧(自動生成）

> 再生成: `node tools/gen-app-map.mjs crud-template`。画面 1 / API 5。手で編集しない。

## 画面(1)

| パス | タイトル |
|---|---|
| `/` | — |

## API(5)

| エンドポイント | メソッド |
|---|---|
| `/api/health` | GET |
| `/api/items` | GET, POST |
| `/api/items/:code` | PUT, PATCH, DELETE |
| `/api/openapi.json` | GET |
| `/api/ready` | GET |
