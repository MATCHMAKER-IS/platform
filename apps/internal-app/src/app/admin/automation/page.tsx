import * as React from "react";
import { AutomationClient } from "./automation-client.js";
export const metadata = { title: "自動化（エクスポート/通知テンプレート）" };
export default function AutomationPage() { return <AutomationClient />; }
