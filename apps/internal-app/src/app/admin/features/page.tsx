import * as React from "react";
import { FeaturesClient } from "./features-client";
export const metadata = { title: "機能アクセス設定" };
export default function FeaturesPage() { return <FeaturesClient />; }
